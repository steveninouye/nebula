const {
  bitwiseNegate,
  createHashValue,
  reverseStr,
  getQueueNumber
} = require('../q/services');

describe('Services', () => {
  it('should be defined', () => {
    expect(createQueues).toBeDefined();
  });
});
